import { createPageMetadata } from '@/lib/seo'
export const metadata = createPageMetadata({ title: 'Social Media & Meta Ads Case Studies', description: 'Explore social media marketing, Meta Ads, creative campaign, and performance results delivered for D\'ECASOFT clients.', path: '/case-studies/social-media-marketing-meta-ads' })
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
