import { createPageMetadata } from '@/lib/seo'
export const metadata = createPageMetadata({ title: 'Digital Marketing Services', description: 'Integrated paid media, social, content, analytics, and conversion strategies that turn marketing investment into measurable growth.', path: '/services/digital-marketing' })
export default function Layout({ children }: { children: React.ReactNode }) { return children }
